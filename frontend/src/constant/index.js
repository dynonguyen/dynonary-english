// routes
export const ROUTES = {
  HOME: '/',
  LOGIN: '/login',
  REGISTER: '/register',
};

export const MAX = {
  EMAIL_LEN: 100,
  PASSWORD_LEN: 40,
};

export const MIN = {};

// theme key
export const THEME_KEYS = {
  ROOT_KEY: 'data-theme',
  LS_KEY: 'theme',
  LIGHT: 'light',
  DARK: 'dark',
};

export const MAX_LEN_SEARCH = 50;

export default {
  MAX_LEN_SEARCH,
  ROUTES,
  THEME_KEYS,
  MAX,
  MIN,
};
